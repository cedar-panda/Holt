# Holt 緩存完整攻略指南（含避坑）
> 2026-07-10 備份。動任何可能影響緩存的代碼之前，先讀完這份。
> 事實源：holt-fix-guide + cache_fix_定案清單 + Session 1-10 實戰記錄。

## 一、架構總覽

```
請求體結構（OpenRouter / Anthropic 風格）：
┌─ system blocks（靜態區）────────────── cache_control 掛最後一個 block
│   staticPart：系統提示 + 角色設定 + 自我註記快照 + 幣值快照
│              + 靜態窗口摘要 pin + 工具區說明 + 習慣快照 + 回覆風格
│   profilePart：用戶資料（可選）
├─ 歷史消息（滾動區）─────────────────── 滾動斷點掛「最新 user 的前一條」
│   從窗口錨點（ctx_anchor）起的全部消息，append-only
└─ 最新 user 消息（動態增量，永不緩存）
    [dynamic block] 記憶注入 + 情緒狀態 + 背包 + 生活狀態 + 時間
    [用戶輸入 block]
```

- 緩存條目 = 從頭到某個 cache_control 斷點的**逐字節前綴**。
- 兩個斷點：① system 靜態區尾部 ② 最新 user 前一條（滾動）。
- 動態內容（記憶/情緒/時間/背包）掛最新 user 上，**在斷點外面**，
  每輪變是設計內的——它變不炸緩存。看到它變就歸因是常見誤診。

## 二、鐵律（違反必炸）

1. **靜態區只放凍結內容**。模型標籤會寫回、再注入 prompt 的數據，先問
   「進靜態還是動態？」靜態注入可變數據一律**快照+增量**：
   開窗凍結快照進靜態，窗內新增走動態增量，下次開窗重拍。
   已中招四次：表情包 prompt、習慣清單、selfNotes、商店清單。
2. **OpenRouter body 禁 `provider.order` / `allow_fallbacks`**——
   會禁用 sticky routing → cache 永不命中。只留 `session_id`。
3. **緩存門檻**：Opus 4.5-4.8 統一 4096 tokens；Sonnet 1024。
   前綴總量不過門檻 = 條目根本不建（write=0 → 永無 read）。
4. **靜態 prompt 任何文本改動 = 上線後第一條重建一次緩存**（一次性、
   預期內）。批量修改一起上，別零碎地一天炸三次。
5. **統計要讀 `prompt_tokens_details.cache_write_tokens`**，
   否則 cache created 顯示不出來。
6. **窗口錨點**（Session 10 新增）：發送歷史從 `ctx_anchor:{convId}`
   （prefs）起載入，**不是**「最新 50 條」。重開窗前綴與上輪逐字節一致。
   錨點只在兩處推進：hysteresis 砍頭時（同步）、超 500 條兜底時。
   **不要**改回 limit:50——那就是「1.5w-2w 必炸」的根因。
7. **渲染與發送解耦**（Session 10）：`_renderFrom` / `_olderMessages`
   只管 UI 顯示，**永不**進發送上下文。改 UI 分頁不會碰緩存；
   反之，任何想「順手」把 _olderMessages 拼進 history 的改動都是事故。
8. **多語系文本**：prompt 文本按 `L.locale` 分支時，**既有語系的分支
   逐字節不動**，只新增其他語系分支。locale 窗口內不變 → 前綴穩定。
   翻譯工具說明時，模型要輸出的**枚舉值/標籤語法保持原文**
   （如 `<emo>` 的中文情緒名），只翻說明文字——否則解析端對不上。

## 三、已定案的機制（勿回退）

| 機制 | 位置 | 要點 |
|---|---|---|
| 靜態窗口摘要 pin | chat_screen `_buildStaticWindowSummaryPrompt` | 綁對話 ID、出生釘死、壓縮 rotate 絕不失效（註釋有「否則又炸緩存」） |
| 快照五件套 | `_buildCommonStaticParts` 的 `??=` | selfNotes / 習慣 / 表情包 / 背包量 / 幣值，開窗凍結 |
| 滾動斷點 | `CacheBreakpoint.applyRolling` | 掛最新 user 前一條；dynamic 注入在 applyRolling **之後** insert 到最新 user |
| dynamic 注入最新 user | 各 service 統一 | 所有 provider 同款；qwen/gemini/openrouter 非 Anthropic 分支已修對 |
| 通話共用聊天前綴 | `_openCall` 構建 bundle 傳 CallScreen | 通話與聊天同 namespace；通話指令拼最後一條 user（斷點外） |
| 保活 ping | keep_alive_service | 重放上輪逐字節前綴；錨點 `_lastRequestAt` 不虛後移 |
| hysteresis 滯後砍頭 | `_hysteresisKeepFrom` | 超上下文上限砍到 60%，砍點持久化為窗口錨點 |
| 通話後重載 | `_loadMessages` | 清空重建（addAll 疊加=雙份=炸），renderFrom 重置 |
| 緩存會話 | `CacheSession.conversationId` | 分支對話返回原窗後要還原 |

## 四、診斷流程（Cache Debug）

開發者診斷 → Cache Debug，每輪請求一條指紋：
- **指紋（static_hash）變了 ⚠️** → 靜態前綴漂移 = 真 bug。
  對比變化那一輪動了什麼（開關切換 / 快照重拍 / 版本更新）。
- **指紋沒變但 ⚡read=0** → TTL 過期（隔太久）或 sticky routing 漂移。
- **static_t = 0 的行** → 摘要壓縮等內部工具請求，不是聊天主請求，忽略。
- 判斷「炸沒炸」看氣泡 ⚡ 與 chip；判斷「為什麼炸」看指紋。

## 五、改動前自查清單（每次動 prompt/歷史相關代碼必跑）

1. 這段文本進靜態還是動態？靜態 → 是否凍結（快照/常量/locale 級穩定）？
2. 改動會不會讓「同一窗口內兩輪請求」的前綴出現任何字節差異？
   → **模擬跑**：在腦內/紙上構造第 1、2 輪請求體，逐段對比。
3. 既有用戶（zh_TW）的文本是否逐字節保留？
4. 歷史消息的來源（錨點窗）有沒有被動到？append-only 有沒有被破壞？
5. 有沒有把每輪變的值（時間/餘額/隨機數/計數）放進靜態或歷史？
6. 改完靜態文本 → 記錄「上線第一條會重建一次緩存（預期）」到修改報告。

## 六、歷史事故簿（血淚索引）

| 事故 | 根因 | 修法 |
|---|---|---|
| selfNotes 每 append 全炸 | 活數據進靜態 | 快照+增量 |
| 表情包/習慣清單炸 | 同上 | 快照 |
| cache created 顯示不出 | 沒讀 cache_write_tokens | 補讀 |
| provider.order 永不命中 | sticky routing 被禁 | 移除 |
| 門檻誤判 | 4.7=2048/4.8=1024 舊值 | 統一 4096 |
| 通話永不命中 | 獨立前綴+不過門檻 | 共用聊天 bundle |
| 1.5w-2w 必炸 | 重開窗 limit:50 滑窗 | 窗口錨點 |
| 通話後炸+雙份 | _loadMessages addAll | 清空重建 |
| 文圖交替必 miss | 兩路徑靜態前綴不一致 | 統一構建 |
