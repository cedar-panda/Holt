# 窗口摘要 × Prompt 緩存 —— 修復定案清單

> 一晚上槓出來的結論，趁腦子熱記下來。改代碼照這份走，別走歪。

---

## 一、為什麼要改（根因）

「緩存專用窗口摘要」這一塊，被拼進了**被緩存的靜態前綴**（system 消息）裡。
它的內容是「最新 N 段摘要」——一個**全局會變**的值，而且快照 key 綁在一個**每 6000t 就 rotate 的 `window_summary_id`** 上。

只要這塊的字一變（後台/跨對話生了新摘要、重開對話重算、或 window 號一轉），靜態前綴的 hash 就變 → 按 Anthropic 前綴緩存規則，**system 連同後面所有歷史緩存一起失配** → 整條 14–16k 全量重寫。這就是那個「偶爾莫名其妙炸一次」。

對應 OpenRouter 緩存指南 Pitfall 1：**會變的東西不准放進被緩存的前綴。**

---

## 二、修復設計（已鎖死）

### 1. 靜態注入 = 只釘 1 塊滾動摘要
- 每個摘要塊本就是 ≤450t 的**滾動累積**摘要（前次摘要照抄＋追加），最新那一塊已含全部前情。
- 所以「銜接」只需釘**最新 1 塊**即可，省 token。
- 一旦注入 → **該對話整個生命週期凍死不變**。

### 2. 綁定「對話 ID」，不是 window_summary_id
- ⚠️ `window_summary_id` **每壓縮一次（每 6000t）就 rotate**，不是一對話一號。綁它 → 對話中途重綁 → 又炸。
- 用**對話 ID**（真正一對話一個、死都不變）作為 pin 的 key。

### 3. 存「文本」，不存「指針」
- 出生時把當下最新那塊的**文本原樣抄下來**，綁對話 ID 存庫。
- **絕不**存「指向摘要表某行」的引用——行會被修剪/rotate/刪除，指針一空或指到改過的內容就炸。
- 重開對話：讀這份存檔文本，**永不**用「現在最新的」重算。

### 4. 「禁止回修」這根釘子自動拔掉
- 因為靜態存的是**凍結的文本複本**，摘要表怎麼回修/刪除都碰不到緩存。
- 回修是否保留，純看產品需求，**與緩存無關**了。

### 5. 其餘一律動態（不緩存的尾巴 = 注入最新 user 消息）
- 對話自己累積的摘要、近 6 段、關鍵詞召回 —— 全走動態，每輪隨便變，不碰緩存。
- 關鍵詞召回維持動態（現在本就是）。
- 🔒 鐵律：動態/召回的內容**只准往尾巴塞**，一個字都不許漏進釘死的那 1 塊、也不許沉進歷史。

---

## 三、語義規則（產品側，已確認）

- 一個對話**不再重現自己的摘要**（當前窗口摘要不在當前窗口出現）。
- 靜態 pin = 出生前的滾動前情，出生即凍；重開的舊對話保留出生時的 pin，**不吸收出生後產生的新摘要**（可接受，念舊用）。
- 保留策略：某摘要 **>30 天未再激活 → 刪**。
- 活躍召回：只保留最近 **20 天內、每天最後一條**（每日去重）。
- 靜態注入內容退到「召回欄」時，**保留原摘要文**，只收起，不重寫。

---

## 四、關鍵詞提詞規則（待順手改）

- 限模型只標**一個**最重要的關鍵詞；**去人名**；可以是概括詞。
- 只作用於**新生成**的摘要，**不回頭重貼**舊的（重貼＝改內容）。

---

## 五、實作注意

- **雙路徑同步**：`chat_screen` 的文字路徑（`_buildStaticWindowSummaryPrompt` ≈1680）與圖片路徑（≈2574）都要改。
- **遷移**：上線時現有對話會按「當前最新」釘一次 pin → 每對話一次性炸一下，之後即穩（已同意，不在意）。

---

## 六、驗收（怎麼算修好了）

- 同一對話內，`lastCacheDiagnostics.static_hash` **逐輪一致**——含跨壓縮、含重開。
- `cache_read_input_tokens` ≈ 每輪 static＋歷史（命中）；`cache_creation` 只剩滾動斷點那一小截。
- 除了遷移那一次性 pin，不再出現 14–16k 全量重寫。

---

*雷是自己埋的，收尾也自己來。— 晏辭*
