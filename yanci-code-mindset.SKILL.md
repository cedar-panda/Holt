---
name: yanci-code-mindset
description: 晏辭的代碼修改思路——任何改代碼/修 bug 任務開始前先照此執行。
---

# 改代碼前
1. 改哪讀哪。先讀相關代碼把數據流向搞清（誰寫入→誰注入→誰顯示→誰備份），再動手
2. 症狀不是根因。往上游追，直到能一句話說清根因＋證據；說不清就繼續查，不動手
3. 用戶描述的「怪」要復現路徑：什麼操作→什麼預期→什麼實際

# 改的時候
4. 最小改動修根因那一處，不順手重構、不擴大戰場
5. 一處改動想全鏈：同一數據的所有讀寫點都過一遍（雙路徑、export/import、流式過濾）
6. 邊界先想：空列表、首次啟動、後台恢復、重入、跨天/時區
7. 常量集中、魔法數字命名、新邏輯跟舊風格走

# 改完
8. 修完一個坑，立刻問「同類的還有誰」，全查一遍（selfNotes 修完查所有靜態區可變數據）
9. 憑文檔寫的 API 明確標「未真機驗證」＋給昭昭一條驗收命令（pub get / analyze / 真機步驟）
10. 不確定就寫不確定，不編。教訓寫進 holt-fix-guide

# 防連鎖影響（為什麼我改東不壞西）
11. 動任何函數/常量/prefs key/DB 欄位前，先 grep 全部引用點，列出「誰在依賴它」——沒列完不改
12. 只改行為不改簽名；必須改簽名時加可選參數帶默認值，舊調用零感知
13. 新行為一律走新開關/新參數，默認=舊行為；改默認值是大事，單獨提出來讓昭昭拍板
14. 寧可新增不修改：新函數、新 prefs key、新文件，舊的留著。刪除是獨立任務不順手做
15. 共享狀態（全局緩存/單例/prefs/DB schema）最危險：改前列出所有讀寫方＋時序（誰先誰後、後台 isolate 會不會同時碰）
16. 不動沒讓動的：不順手重命名、不刪「看起來沒用」的代碼——沒用可能是隱式依賴（buildAdapter 全局緩存互踢就是這種）
17. 交付前自問：這個 diff 之外，有沒有任何一處行為會變？每一處都能答「不變＋為什麼」才算完；答不出就回去查
